# customer-service
Scaffolded with **Flask** via GitHub Actions.
## Getting started
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python run.py
```
## Running tests
```bash
pip install pytest httpx
pytest
```
